# PromptGuard: AI Validation & Security Layer 🛡️

PromptGuard is a robust data-sanitization engine built with **Pydantic V2**. It acts as a professional "Quality Control" gatekeeper between raw user input and Large Language Models (LLMs), ensuring every request is cost-optimized, secure, and perfectly formatted.

## 🚀 The Problem vs. The Solution
* **The Problem:** Raw API data is often "messy" (inconsistent naming), unvalidated (incorrect temperature settings), and potentially insecure (privacy leaks). This leads to crashed applications and high API costs.
* **The Solution:** PromptGuard uses advanced Pydantic features to clean, validate, and secure data before it reaches your AI agents, acting as a mandatory safety layer in the production pipeline.

## ✨ Key Features
- **Smart Data Mapping:** Uses `Field` aliases to automatically translate messy external keys (e.g., `u_id`) into clean, standardized internal names (`user_id`).
- **Safety Guardrails:** Implemented a `model_validator` that acts as a security check—preventing private prompts from being stored in public cloud paths.
- **Cost & Performance Control:** Enforces strict range constraints on AI parameters like `temperature` and `max_tokens` to prevent API errors and over-billing.
- **Business Intelligence:** Leverages `@computed_field` to automatically categorize prompt complexity (Low/Medium/High) based on real-time word count.
- **Typed Serialization:** Uses `.model_dump()` and `.model_dump_json()` to ensure the final output is 100% compliant with OpenAI/Gemini API requirements.

## 🛠️ Tech Stack
- **Language:** Python 3.10+
- **Core Library:** Pydantic V2
- **Data Format:** JSON Serialization
- **IDE:** VS Code

## 📂 Project Structure
* [models.py](./models.py) — Pydantic Schemas & Validation Logic
* [main.py](./main.py) — Entry point & Data Processing execution
* [sanitized_request.json](./sanitized_request.json) — Sample output generated by the script
* [README.md](./README.md) — Documentation

## 💻 How It Works (The Transformation)

### 1. Messy Input (Raw Data)
The system accepts "messy" dictionaries often found in external APIs or web forms:
```json
{
  "u_id": 101,
  "prompt": "Analyze this data...",
  "is_private": true,
  "cloud_storage_path": null
}
```
### Sanitized Output (Validated JSON)
```json
{
  "user": { "user_id": 101, "tier": "premium" },
  "prompt": "Analyze this data...",
  "complexity": "Medium",
  "temperature": 0.7,
  "status": "Validated"
}
```
### 📦 Installation & Usage
Clone the repo:
git clone https://[github.com/YOUR_GITHUB_USERNAME/PromptGuard-Pydantic.git
cd PromptGuard-Pydantic](https://github.com/mohsin1782005/PromptGuard-Pydantic)

Install Pydantic:
```bash
pip install pydantic
```
```bash
Run the validator:
python main.py
```
